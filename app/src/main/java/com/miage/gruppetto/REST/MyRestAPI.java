package com.miage.gruppetto.REST;

import android.os.AsyncTask;
import android.util.Log;

public class MyRestAPI extends AsyncTask<Void, Void, String> {

    @Override
    protected String doInBackground(Void... voids) {
        return null;
    }
}
